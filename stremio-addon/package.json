{
  "name": "kazumi-stremio-addon",
  "version": "0.1.0",
  "private": true,
  "description": "Stremio-compatible bridge for Kazumi-style media sources",
  "type": "module",
  "license": "GPL-3.0-only",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "start": "node src/server.mjs",
    "dev": "node --watch src/server.mjs",
    "check": "node --check src/model.mjs && node --check src/addon.mjs && node --check src/server.mjs && node --check scripts/build-static.mjs",
    "test": "node --test",
    "build:static": "node scripts/build-static.mjs",
    "verify": "npm run check && npm test && npm run build:static"
  },
  "files": [
    "src/",
    "scripts/build-static.mjs",
    "Dockerfile",
    "compose.yaml",
    "README.md"
  ]
}
